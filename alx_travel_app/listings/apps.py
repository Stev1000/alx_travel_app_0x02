from django.apps import AppConfig

class ListingsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'alx_travel_app.listings'  # ✅ This must match your INSTALLED_APPS
